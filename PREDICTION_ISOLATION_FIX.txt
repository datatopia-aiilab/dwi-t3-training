═══════════════════════════════════════════════════════════════════════════
PREDICTION ISOLATION FIX - แก้ปัญหาภาพ Predictions ปนกัน
═══════════════════════════════════════════════════════════════════════════

Implementation Date: November 9, 2025
Status: ✅ Complete

═══════════════════════════════════════════════════════════════════════════
ปัญหาที่พบ (PROBLEM)
═══════════════════════════════════════════════════════════════════════════

❌ ตอน Evaluate มีรูป predictions เยอะจัง
❌ มี case และ slice เดิมซ้ำกัน
❌ มาจากการ train model หลายรอบแล้วภาพค้างอยู่
❌ ไม่มีการแยก predictions ตาม run

สาเหตุ:
-------
1. ใช้ PREDICTIONS_DIR เดียวกันทุก run
2. ไม่มีการล้างภาพเก่าก่อน train ใหม่
3. ภาพจากทุก run รวมกันใน folder เดียว
4. ไม่สามารถระบุได้ว่าภาพไหนมาจาก run ไหน

═══════════════════════════════════════════════════════════════════════════
วิธีแก้ (SOLUTION)
═══════════════════════════════════════════════════════════════════════════

✅ สร้าง subfolder แยกตาม MLflow run_id
✅ ล้างภาพเก่าอัตโนมัติก่อน train
✅ แต่ละ run มี predictions folder เป็นของตัวเอง
✅ เพิ่ม run_id tags ใน MLflow

โครงสร้างใหม่:
--------------
4_results/
├── predictions/
│   ├── run_a1b2c3d4/    ← Run 1 (แยกอิสระ)
│   │   ├── Patient_001_Slice_010.png
│   │   ├── Patient_002_Slice_015.png
│   │   └── ...
│   ├── run_e5f6g7h8/    ← Run 2 (แยกอิสระ)
│   │   ├── Patient_001_Slice_010.png
│   │   └── ...
│   └── run_i9j0k1l2/    ← Run 3 (แยกอิสระ)
│       └── ...
├── plots/
└── ...

═══════════════════════════════════════════════════════════════════════════
การเปลี่ยนแปลง (CHANGES)
═══════════════════════════════════════════════════════════════════════════

1. config.py
   ----------
   เพิ่ม comments อธิบายการแยก predictions:
   
   ```python
   PREDICTIONS_DIR = RESULTS_DIR / "predictions"
   # Note: PREDICTIONS_DIR will be updated to run-specific subfolder
   # e.g., predictions/run_a1b2c3d4/ to separate from different runs
   ```
   
   เพิ่มส่วน MLflow Run Isolation:
   
   ```python
   # MLflow Run Isolation
   # แต่ละ training run จะมี:
   # 1. Unique run_id (e.g., a1b2c3d4e5f6g7h8)
   # 2. Separate prediction folder: predictions/run_{run_id[:8]}/
   # 3. Independent metrics, parameters, and artifacts in MLflow
   # 4. No cross-contamination between runs
   # → แต่ละ run เป็นอิสระ ไม่มีการเก็บข้อมูลข้าม run กัน
   ```

2. train.py
   --------
   เพิ่มการล้างภาพเก่าและสร้าง run-specific directory:
   
   ```python
   # Clean old predictions and create run-specific directory
   print("\n🗑️  Preparing prediction directory...")
   if cfg.MLFLOW_ENABLED and mlflow_run:
       # Create run-specific subdirectory
       run_id = mlflow_run.info.run_id[:8]  # First 8 chars
       run_predictions_dir = cfg.PREDICTIONS_DIR / f"run_{run_id}"
       run_predictions_dir.mkdir(parents=True, exist_ok=True)
       
       # Clean old files in run directory
       old_files = list(run_predictions_dir.glob("*.png"))
       if old_files:
           for f in old_files:
               f.unlink()
           print(f"   🗑️  Cleaned {len(old_files)} old predictions")
       
       # Update config
       cfg.PREDICTIONS_DIR = run_predictions_dir
       print(f"   📁 Predictions → {run_predictions_dir.name}/")
   else:
       # If MLflow disabled, ask user
       old_files = list(cfg.PREDICTIONS_DIR.glob("*.png"))
       if old_files:
           response = input("   Delete old predictions? (y/n): ")
           if response == 'y':
               for f in old_files:
                   f.unlink()
   ```
   
   อัปเดต print statement ให้แสดงข้อมูลชัดเจน:
   
   ```python
   rel_path = cfg.PREDICTIONS_DIR.relative_to(cfg.PROJECT_ROOT)
   print(f"      ✅ ALL {len(pred_images)} test samples")
   print(f"      📁 Location: {rel_path}/")
   if mlflow_run:
       print(f"         - Run ID: {mlflow_run.info.run_id[:8]}")
   ```

3. mlflow_utils.py
   ---------------
   เพิ่ม run_id เป็น tags:
   
   ```python
   mlflow.set_tag("run_id", run.info.run_id)
   mlflow.set_tag("run_id_short", run.info.run_id[:8])
   ```

═══════════════════════════════════════════════════════════════════════════
วิธีใช้งาน (USAGE)
═══════════════════════════════════════════════════════════════════════════

การ Train:
----------
```bash
python train.py
```

Output จะแสดง:
```
🗑️  Preparing prediction directory...
   🗑️  Cleaned 48 old prediction images from this run
   📁 Predictions will be saved to: run_a1b2c3d4/

...training...

📁 GENERATED FILES:
   Prediction Images:
      ✅ ALL 48 test samples visualized
      📁 Location: 4_results/predictions/run_a1b2c3d4/
         - Format: 4 panels
         - Resolution: 300 DPI
         - Run ID: a1b2c3d4
```

การดู Predictions แต่ละ Run:
----------------------------
```bash
# ดู predictions จาก run ล่าสุด
ls 4_results/predictions/run_*/

# ดู predictions จาก run เฉพาะ
ls 4_results/predictions/run_a1b2c3d4/

# เปรียบเทียบ run ต่าง ๆ
open 4_results/predictions/run_a1b2c3d4/Patient_001_Slice_010.png
open 4_results/predictions/run_e5f6g7h8/Patient_001_Slice_010.png
```

การค้นหา Run ID ใน MLflow:
---------------------------
```bash
mlflow ui --port 5000

# แล้วเปิด http://localhost:5000
# Filter by tags: run_id_short = a1b2c3d4
```

═══════════════════════════════════════════════════════════════════════════
ประโยชน์ (BENEFITS)
═══════════════════════════════════════════════════════════════════════════

✅ แต่ละ run มี predictions แยกกันอย่างชัดเจน
✅ ไม่มีภาพปนกันจาก run ต่าง ๆ
✅ ง่ายต่อการเปรียบเทียบผลลัพธ์
✅ สามารถ trace กลับไป run ต้นทางได้
✅ MLflow จัดการ artifacts อย่างเป็นระบบ
✅ ไม่ต้องลบภาพเก่าด้วยตัวเอง

═══════════════════════════════════════════════════════════════════════════
ตัวอย่างการใช้งาน (EXAMPLES)
═══════════════════════════════════════════════════════════════════════════

Scenario 1: เปรียบเทียบ 3 runs ต่างกัน
----------------------------------------
Run 1 (warmup_cosine):
  → predictions/run_a1b2c3d4/
  → Val Dice: 0.68

Run 2 (adaptive):
  → predictions/run_e5f6g7h8/
  → Val Dice: 0.65

Run 3 (onecycle):
  → predictions/run_i9j0k1l2/
  → Val Dice: 0.71 ⭐ Best!

เปรียบเทียบ predictions:
```bash
# เปิดภาพเดียวกันจาก 3 runs
open predictions/run_a1b2c3d4/Patient_005_Slice_020.png
open predictions/run_e5f6g7h8/Patient_005_Slice_020.png
open predictions/run_i9j0k1l2/Patient_005_Slice_020.png
```

Scenario 2: Debug specific case
--------------------------------
ถ้า Patient_010_Slice_015 มีปัญหา:

1. ค้นหา run ที่มี Dice ต่ำสุด:
   ```bash
   # ใน MLflow UI
   Sort by: metrics.test_dice (ascending)
   ```

2. ดู predictions:
   ```bash
   cd predictions/run_{worst_run_id}/
   open Patient_010_Slice_015.png
   ```

3. เปรียบเทียบกับ run ที่ดีที่สุด:
   ```bash
   cd predictions/run_{best_run_id}/
   open Patient_010_Slice_015.png
   ```

Scenario 3: Track progression across experiments
-------------------------------------------------
ติดตาม improvement ของ architecture เดียวกัน:

Experiment 1: UNet++ baseline
  - Run 1: predictions/run_aaa11111/ → Dice 0.60
  - Run 2: + SE → predictions/run_bbb22222/ → Dice 0.62
  - Run 3: + CBAM → predictions/run_ccc33333/ → Dice 0.65
  - Run 4: + Warmup → predictions/run_ddd44444/ → Dice 0.68

แต่ละ run มีภาพแยกชัดเจน ไม่ปน!

═══════════════════════════════════════════════════════════════════════════
การทำความสะอาด (CLEANUP)
═══════════════════════════════════════════════════════════════════════════

ลบ runs เก่าที่ไม่ต้องการ:
--------------------------
```bash
# ลบ predictions จาก run เฉพาะ
rm -rf 4_results/predictions/run_a1b2c3d4/

# ลบทุก run เก่า (เก็บเฉพาะ 3 runs ล่าสุด)
cd 4_results/predictions/
ls -t | tail -n +4 | xargs rm -rf

# ลบทั้งหมด (เริ่มใหม่)
rm -rf 4_results/predictions/run_*/
```

ลบ MLflow runs:
---------------
```bash
# ใน MLflow UI
1. Select runs to delete
2. Click "Delete" button
3. Confirm deletion
```

═══════════════════════════════════════════════════════════════════════════
Q&A
═══════════════════════════════════════════════════════════════════════════

Q: ถ้าปิด MLflow จะเป็นยังไง?
A: ระบบจะถาม user ว่าต้องการลบภาพเก่าไหม
   - y = ลบภาพเก่า
   - n = เก็บภาพเก่าไว้ (อาจปนกัน)

Q: run_id ยาวมั้ย?
A: Full run_id ยาว 32 chars แต่ใช้ 8 chars แรก (run_a1b2c3d4)
   เพียงพอสำหรับการแยก แต่ยังสั้นและอ่านง่าย

Q: ภาพเก่าจะถูกลบทุกครั้งไหม?
A: ใช่ ก่อน train ใหม่จะลบภาพเก่าใน run_id นั้น ๆ
   แต่ run อื่น ๆ จะไม่ถูกแตะต้อง

Q: ถ้า train ซ้ำด้วย run_id เดิม?
A: ไม่เกิดขึ้น MLflow สร้าง run_id ใหม่ทุกครั้ง

Q: จะหา predictions จาก run เฉพาะได้ยังไง?
A: 1. ดู run_id ใน MLflow UI
   2. ไปที่ predictions/run_{run_id}/
   หรือดูจาก terminal output ตอน train เสร็จ

═══════════════════════════════════════════════════════════════════════════
สรุป (SUMMARY)
═══════════════════════════════════════════════════════════════════════════

✅ แก้ไขปัญหาภาพ predictions ปนกันครบถ้วน
✅ แต่ละ run มี folder แยกตาม run_id
✅ ล้างภาพเก่าอัตโนมัติก่อน train
✅ เพิ่ม run_id tags ใน MLflow
✅ แสดงข้อมูล run_id ชัดเจนใน output
✅ ระบบ isolation สมบูรณ์

การเปลี่ยนแปลงนี้ทำให้:
- แต่ละ training run เป็นอิสระสมบูรณ์
- ไม่มีการเก็บข้อมูลข้าม run กัน
- MLflow จัดการทุกอย่างอย่างเป็นระบบ
- ง่ายต่อการเปรียบเทียบและ debug

═══════════════════════════════════════════════════════════════════════════
